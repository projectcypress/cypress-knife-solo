site :opscode

cookbook 'popHealth', git: 'https://github.com/rbclark/popHealth-recipe.git'
# Since Berks resolves dependencies using the metadata.rb file instead of the Berksfile.lock, we must specify RVM here as well.
cookbook 'rvm', github: 'fnichol/chef-rvm', ref: '0cd320911c1e568cbf2eeac895a5ef06e5821589'